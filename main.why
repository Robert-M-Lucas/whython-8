struct LL {
    has_node: bool,
    first: &Node
}


struct Node {
    val: int,
    has_next: bool,
    next: &Node
}

fn main() -> int {
    let r: LL = LL { has_node: false, first: Node::null() };
}