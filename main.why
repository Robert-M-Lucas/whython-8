fn fib(a: int, b: int) {
    printi(a);

    if (a == 233) {
        exit(10);
    }

    let c: int = b;
    let d: int = a + b;

    fib(c, d);
}

fn main() -> int {
    fib(0, 1);

    return 1;
}
