fn fib(a: int, b: int) {
    printi(b);

    if (a == 24157817) {
        exit(10);
    }

    let c: int = b;
    let d: int = a + b;

    fib(c, d);
}

fn main() -> int {
    printi(-100000000000);
    exit(2);

    fib(0, 1);


    return 1;
}
